- content_for :content do
  %div
    = link_to_function "go simple cached", "NiceEventDispatcher.dispatch_event('nice.state.CachedTransitionEvent',{transition_id : 'get_many_nice__get_simple_nice'})"
    .two{"data-state" => "get_nice_many"}
      %h1 Only Many
      = yield(:container1)
    .one{"data-state" => "get_nice_simple"}
      %h1 Only Simple
      = yield(:container1)
    .hello
      %p ein bisschen witz
    - 3.times do
      %div{"data-state" => "get_nice_simple get_nice_many", "data-state-update" => :yes}
        = yield(:container2)

= render :template => 'layouts/application'