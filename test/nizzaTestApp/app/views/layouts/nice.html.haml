- content_for :content do
  %div
    = link_to_function "go simple cached", "var evt = document.createEvent('Event'); evt.initEvent('nice.state.CachedTransitionEvent',true,true); evt.transition_id = 'get_many_nice__get_simple_nice'; document.dispatchEvent(evt)"
    .two{"data-state" => "get_nice_many"}
      %h1 Only Many
      = yield(:container1)
    .one{"data-state" => "get_nice_simple"}
      %h1 Only Simple
      = yield(:container1)
    .hello
      %p ein bisschen witz
    - 3.times do
      %div{"data-state" => "get_nice_simple get_nice_many", "data-state-update" => :yes}
        = yield(:container2)

= render :template => 'layouts/application'